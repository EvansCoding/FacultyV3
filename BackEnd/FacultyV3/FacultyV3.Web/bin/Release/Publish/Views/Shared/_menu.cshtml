<section id="mu-menu">
    <nav class="navbar navbar-default" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <!-- FOR MOBILE VIEW COLLAPSED BUTTON -->
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="@Url.Action("Index","Home", new { Area = String.Empty})">
                    <img class="image-logo" src="~/Content/Client/assets/img/Banner1.jpg" alt="logoKhoa" />
                </a>
            </div>  

            <div id="navbar" class="navbar-collapse collapse" style="">
                <ul id="top-menu" class="nav navbar-nav navbar-right main-nav">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Giới thiệu <span class="fa fa-angle-down"></span>
                        </a>
                        @{
                            if (ViewBag.intro != null)
                            {
                                if (Enumerable.Count(ViewBag.intro) > 0)
                                {
                                    <ul class="dropdown-menu" role="menu">
                                        @foreach (var item in ViewBag.intro as List<FacultyV3.Core.Models.Entities.Category_Menu>)
                                        {
                                            <li><a href="@item.Meta_Value">@item.Meta_Name</a></li>
                                        }
                                    </ul>
                                }
                            }
                        }
                    </li>
                    @{
                        if (ViewBag.admission != null)
                        {
                            var admission = ViewBag.admission as FacultyV3.Core.Models.Entities.Category_Menu;
                            <li><a href="@admission.Meta_Value">@admission.Meta_Name</a></li>

                        }
                    }

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Đào tạo <span class="fa fa-angle-down"></span>
                        </a>
                        @{
                            if (ViewBag.educate != null)
                            {
                                if (Enumerable.Count(ViewBag.educate) > 0)
                                {
                                    <ul class="dropdown-menu" role="menu">

                                        @foreach (var item in ViewBag.educate as List<FacultyV3.Core.Models.Entities.Category_Menu>)
                                        {
                                            <li><a href="@item.Meta_Value">@item.Meta_Name</a></li>
                                        }
                                    </ul>
                                }
                            }
                        }
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Nghiên cứu <span class="fa fa-angle-down"></span>
                        </a>
                        @{
                            if (ViewBag.research != null)
                            {
                                if (Enumerable.Count(ViewBag.research) > 0)
                                {
                                    <ul class="dropdown-menu" role="menu">

                                        @foreach (var item in ViewBag.research as List<FacultyV3.Core.Models.Entities.Category_Menu>)
                                        {
                                            <li><a href="@item.Meta_Value">@item.Meta_Name</a></li>
                                        }
                                    </ul>
                                }
                            }
                        }
                    </li>
                    @{
                        if (ViewBag.resource != null)
                        {

                            var resource = ViewBag.resource as FacultyV3.Core.Models.Entities.Category_Menu;
                            <li><a href="@resource.Meta_Value">@resource.Meta_Name</a></li>

                        }
                    }

                    @{
                        if (ViewBag.party_cell != null)
                        {

                            var resource = ViewBag.party_cell as FacultyV3.Core.Models.Entities.Category_Menu;
                            <li><a href="@resource.Meta_Value">@resource.Meta_Name</a></li>

                        }
                    }

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Cựu sinh viên <span class="fa fa-angle-down"></span>
                        </a>
                        @{
                            if (ViewBag.alumni != null)
                            {
                                if (Enumerable.Count(ViewBag.alumni) > 0)
                                {
                                    <ul class="dropdown-menu" role="menu">

                                        @foreach (var item in ViewBag.alumni as List<FacultyV3.Core.Models.Entities.Category_Menu>)
                                        {
                                            <li><a href="@item.Meta_Value">@item.Meta_Name</a></li>
                                        }
                                    </ul>
                                }
                            }
                        }
                    </li>
                </ul>
            </div>
            <!--/.nav-collapse -->
        </div>
    </nav>
</section>
